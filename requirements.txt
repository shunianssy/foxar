# 整合所有依赖项
# 主项目依赖
-e .[all]

# 通用依赖
pre-commit >=2.17.0,<5.0.0
playwright

# 测试依赖
pytest >=7.1.3,<9.0.0
coverage[toml] >= 6.5.0,< 8.0
mypy ==1.14.1
dirty-equals ==0.9.0
sqlmodel==0.0.24 --force-reinstall
flask >=1.1.2,<4.0.0
anyio[trio] >=3.2.1,<5.0.0
PyJWT==2.9.0
pyyaml >=5.3.1,<7.0.0
inline-snapshot>=0.21.1
types-ujson ==5.10.0.20240515
types-orjson ==3.6.2

# 文档依赖
mkdocs-material==9.6.16
mdx-include >=1.4.1,<2.0.0
mkdocs-redirects>=1.2.1,<1.3.0
typer == 0.16.0
jieba==0.42.1
pillow==11.3.0
mkdocstrings[python]==0.26.1
griffe-typingdoc==0.2.8
black==25.1.0
mkdocs-macros-plugin==1.3.9
markdown-include-variants==0.0.4

# 文档测试依赖
httpx >=0.23.0,<1.0.0
ruff ==0.12.7

# 可能需要编译的包，使用预编译二进制包
passlib[bcrypt] >=1.7.2,<2.0.0 --only-binary :all:
cairosvg==2.8.2 --only-binary :all: